<ui:FluentWindow x:Class="Prosmotr.Views.TorrentCacheWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 Title="Кэш магнет-стриминга"
                 Width="640" SizeToContent="Height"
                 WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
                 ExtendsContentIntoTitleBar="True">

    <StackPanel Margin="28,8,28,24">
        <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
            <ui:SymbolIcon Symbol="Broom24" FontSize="20" Opacity="0.8" VerticalAlignment="Center" Margin="0,0,10,0" />
            <TextBlock Text="Кэш магнет-стриминга" FontSize="17" FontWeight="SemiBold" VerticalAlignment="Center" />
        </StackPanel>

        <TextBlock x:Name="PathText" FontSize="12" Opacity="0.7" TextWrapping="Wrap" Margin="0,0,0,12" />

        <TextBlock x:Name="SummaryText" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8" />

        <TextBlock Text="Здесь лежат скачанные по магнет-ссылкам файлы (это сам фильм, без сжатия — 10 ГБ фильм = ~10 ГБ кэша). Пока приложение открыто и сессия активна, файл раздаётся другим."
                   Opacity="0.6" FontSize="12" TextWrapping="Wrap" Margin="0,0,0,10" />

        <Border Background="#1AFFFFFF" CornerRadius="6" Padding="10,8" Margin="0,0,0,14">
            <ItemsControl x:Name="TorrentsList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,2">
                            <TextBlock Text="{Binding Name}" TextTrimming="CharacterEllipsis" MaxWidth="440"
                                       HorizontalAlignment="Left" VerticalAlignment="Center" />
                            <TextBlock Text="{Binding Size}" HorizontalAlignment="Right" Opacity="0.7"
                                       VerticalAlignment="Center" />
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Border>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <ui:Button Content="Закрыть" MinWidth="110" Click="OnClose"
                       AutomationProperties.Name="Закрыть" />
            <ui:Button x:Name="ClearButton" Content="Очистить кэш" Appearance="Primary" MinWidth="150"
                       Margin="10,0,0,0" Click="OnClear"
                       AutomationProperties.Name="Очистить кэш">
                <ui:Button.Icon><ui:SymbolIcon Symbol="Broom24" /></ui:Button.Icon>
            </ui:Button>
        </StackPanel>

        <TextBlock x:Name="StatusText" FontSize="12" Opacity="0.75" Margin="0,10,0,0" TextWrapping="Wrap" />
    </StackPanel>
</ui:FluentWindow>
