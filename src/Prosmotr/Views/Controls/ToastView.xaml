<UserControl x:Class="Prosmotr.Views.Controls.ToastView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             IsHitTestVisible="True" Background="{x:Null}">

    <!-- Сам контрол прозрачен и не перехватывает клики мимо плашки (Background=null).
         Плашка прижата к верхнему центру области и анимируется в коде. -->
    <Grid>
        <Border x:Name="Root" Visibility="Collapsed" Opacity="0"
                HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,16,0,0"
                MaxWidth="560" MinHeight="44" CornerRadius="10"
                Background="#F0202020" BorderThickness="1" BorderBrush="#33FFFFFF"
                Padding="14,8">
            <Border.RenderTransform>
                <TranslateTransform x:Name="Slide" Y="-14" />
            </Border.RenderTransform>
            <Border.Effect>
                <DropShadowEffect BlurRadius="18" ShadowDepth="0" Opacity="0.5" />
            </Border.Effect>

            <StackPanel Orientation="Horizontal">
                <ui:SymbolIcon x:Name="Icon" Symbol="Info24" FontSize="18"
                               Foreground="White" VerticalAlignment="Center" Margin="0,0,10,0" />
                <TextBlock x:Name="MessageText" Foreground="White" VerticalAlignment="Center"
                           TextWrapping="Wrap" MaxWidth="420" />
                <ui:Button x:Name="ActionButton" Appearance="Secondary" Margin="14,0,0,0"
                           Padding="12,4" VerticalAlignment="Center" Visibility="Collapsed"
                           Click="OnActionClick" />
                <ui:Button x:Name="CloseButton" Appearance="Transparent" Margin="6,0,0,0"
                           Padding="6" VerticalAlignment="Center" Click="OnCloseClick"
                           ToolTip="Закрыть">
                    <ui:Button.Icon><ui:SymbolIcon Symbol="Dismiss24" FontSize="14" Foreground="White" /></ui:Button.Icon>
                </ui:Button>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
